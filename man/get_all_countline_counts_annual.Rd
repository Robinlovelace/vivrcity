% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_retrieval.R
\name{get_all_countline_counts_annual}
\alias{get_all_countline_counts_annual}
\title{Retrieves count data for all available countlines over a one-year period.
This function iterates through all countlines found in metadata and calls
\code{get_counts()} for each, combining the results into a single data frame.}
\usage{
get_all_countline_counts_annual(end_date = Sys.time(), time_bucket = "24h")
}
\arguments{
\item{end_date}{The end date for the one-year period (POSIXct object).
Defaults to the current system time.}

\item{time_bucket}{Time bucket size (e.g. "1h", "5m"). Defaults to "24h".}
}
\value{
A data frame of counts.
}
\description{
Retrieves count data for all available countlines over a one-year period.
This function iterates through all countlines found in metadata and calls
\code{get_counts()} for each, combining the results into a single data frame.
}
