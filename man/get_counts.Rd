% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counts.R
\name{get_counts}
\alias{get_counts}
\title{Get Countline Counts}
\usage{
get_counts(
  countline_ids,
  from,
  to,
  by_class = TRUE,
  split_direction = TRUE,
  aggregate = FALSE,
  time_bucket = "24h",
  wait = 1
)
}
\arguments{
\item{countline_ids}{Vector of countline IDs.}

\item{from}{Start timestamp.}

\item{to}{End timestamp.}

\item{by_class}{If TRUE (default), returns counts broken down by transport
class (pedestrian, cyclist, etc.) in long format. If FALSE, returns
total counts only.}

\item{split_direction}{If TRUE (default), preserves direction information.
If FALSE, sums counts across directions.}

\item{aggregate}{If TRUE, aggregates the results by sensor (summing directions
and combining countlines belonging to the same sensor). Defaults to FALSE.}

\item{time_bucket}{Time bucket size (e.g. "1h", "5m"). Defaults to "24h".}

\item{wait}{Seconds to wait between API requests. Defaults to 1.}
}
\value{
A data frame with columns \code{id}, \code{sensor_name}, \code{name}, \code{from}, \code{to}, \code{class}, \code{direction}, \code{count}.
If \code{by_class} is FALSE, \code{class} will be "all".
If \code{split_direction} is FALSE, \code{direction} column is omitted (counts are summed).
If \code{aggregate} is TRUE, \code{id} column will contain the sensor name, and the \code{direction} and \code{name} columns are removed.
}
\description{
Simple wrapper to get countline counts with class breakdown by default.
This is the recommended function for most use cases.
It automatically fetches metadata to add \code{name} and \code{sensor_name} columns,
enabling easier aggregation by sensor.
}
